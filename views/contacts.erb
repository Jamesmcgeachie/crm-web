<h1>All Contacts</h1>
<div id="all">
	<table >
		<% Contact.all.each do |contact| %>
			<tr>
			  <td><a href="contacts/<%= contact.id %>"><%= contact.full_name %></a></td>
			  <td><%= contact.email %></td>
			  <td><%= contact.notes %></td>
			</tr>
		<% end %>
	</table>
</div>